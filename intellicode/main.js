const userName = "Leto";
const userLastName = "Atredis";
const userFullName = userName.concat("", userLastName);
const userEmail = "leto@atredis.com";
const currentDate = new Date().toUTCString
/* Con la bombilla nos permite ver proyecto con el nombre de esa función sobre
otros proyecto, y en que repositorios se esta basando. */

const printValue (userName, userLastName, userFullName, currentDate) => {
    console.log(userName);
    console.log(userLastName);
    console.log(userFullName);
    console.log(currentDate);
}
 